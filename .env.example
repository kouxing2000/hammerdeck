# Local dev secrets for Hammerdeck -- copy this file to `.env` and fill in.
# `.env` is gitignored; NEVER commit real values.

# OpenAI API key, used by the `text_actions` feature for its LLM-backed actions.
# Get one at https://platform.openai.com/api-keys
OPENAI_KEY=

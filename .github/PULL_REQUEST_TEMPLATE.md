## What this changes

## Why

## Testing
- [ ] `lua test/run.lua` passes
- [ ] `scripts/test-lua.sh` passes (if Lua changed)
- [ ] `swift test` passes (if Swift / seam changed)
- [ ] Verified in the running app (for UI / behavior changes)

## Notes
<!-- If this adds a new feature, link the issue where it was discussed first. -->

{
  "name": "Window Rewind",
  "version": "1.0.0",
  "description": "Undo for any window move: one hotkey (Hyper+Z) restores every window a snap, screen-swap, grid, or deck move just repositioned, and returns the pointer with them. Single-step -- it rewinds the most recent change.",
  "category": "windows",
  "order": 6,
  "icon": "arrow.uturn.backward",
  "context": "window",
  "defaultEnabled": true,
  "recommended": true,
  "requires": ["accessibility"],
  "preview": {
    "archetype": "windowRewind"
  }
}

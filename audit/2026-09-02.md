# Repository audit -- 2026-09-02

Head audited: `618596a`. Environment: a Linux coding session, so everything here
is code reading plus the headless Lua suite on the vendored 5.4.7 engine. The
Swift build, the UI tests, the live feature matrix and profiling need a Mac;
they are listed at the end with what each should settle.

Scope was chosen to avoid re-checking what the repo already machine-checks.
Capability declarations (both directions), the feature require allowlist, i18n
parity, LuaLS types at Error level, the subprocess-site roster, README catalog
sync and the business-content hook are all gated in CI already. This looked at
what no guard covers: the trust boundaries, error containment, teardown, the
docs' factual claims, and the shape of the test coverage itself.

## Verdict

**Corrected 2026-09-02, second pass.** The first version of this report said "no
S1 or S2 finding". That verdict was true of what had been READ, and the reading
was incomplete: three of the six parallel readers died on a rate limit mid-run
and were never replaced before the report was written, so the pass was strong on
security and thin on whether the code computes the right answer. Re-running those
three found **six S2 defects** -- user-visible wrong behavior, including one host
crash. The security conclusions below stand unchanged; the functional verdict did
not.

On the security half: no trust-boundary bypass, secret exposure or
crash-from-hostile-input was found. The seam design holds up under adversarial
reading -- the MCP endpoint, the `exec` tier, the AppleScript and JXA builders,
the capability gate, the Lua callback error path and the release signing flow all
do what the docs say.

| Severity | First pass | Second pass | Total | Fixed | Open |
|---|---|---|---|---|---|
| S1 -- boundary bypass, secret exposure, data loss | 0 | 0 | 0 | -- | 0 |
| S2 -- user-visible wrong behavior or crash | 0 | 8 + 3 (P) | 11 | 8 | 3 |
| S3 -- latent bug, missing guard, drift | 10 + F5b | 21 + 7 (P) | 39 | 16 | 23 |
| S4 -- hygiene | 7 | 14 + 3 (P) | 24 | 6 | 18 |
| **Total** | **18** | **43 + 13 (P)** | **74** | **30** | **44** |

Counted from the tables below rather than tallied by hand -- an earlier revision
of this row said six S2, having dropped H-5 (the AX observers registered in
`.defaultMode`) out of the count. The eight S2 findings are N-1, N-3, N-6, W-1,
W-1b, W-8, H-1 and H-5; seven are fixed and only N-6 remains open. Of the
platform pass's three, P-1 is fixed and P-2 and P-3 stay open as design calls --
so three S2 findings are open in total: N-6, P-2, P-3.

W-1b is the only finding here that no audit reader found. It surfaced in the
post-fix REVIEW of W-1 (seventh round below), which is the argument for
reviewing a fix against its own defect class rather than against its diff.

Second-pass findings carry an `N-` (features), `W-` (window features) or `H-`
(Swift host) prefix and are listed in their own section below. Every S2 in that
set was re-verified by hand against the code before being written down here.

## Fixed -- first round (committed with this report)

| ID | What was wrong | What changed |
|---|---|---|
| F2 | README asserted every feature ships off by default; Confirm Shortcut Presses has `defaultEnabled: true` (`app/features/confirm_shortcut/feature.json`), which `registry.lua:414` honors. | `scripts/gen-readme-features.py` now DERIVES the sentence from the catalog and names the default-on features. CI runs `--check`, so the claim can no longer drift from the code. |
| F3 | SECURITY.md's permissions table claimed an Input Monitoring grant for Caps->Hyper and chords. Both halves were wrong: the Caps->Hyper tap is a `.cgSessionEventTap` that rewrites events and needs **Accessibility** (`CapsHyperTap.swift:28` says so itself), and chords are Carbon `RegisterEventHotKey` hotkeys needing **no permission** (`ChordCenter.swift:7`). | Row rewritten: Accessibility now covers window control, keystroke synthesis and the Caps->Hyper remap. A following line states that chords need no permission. |
| F5 | `scripts/publish-site.sh:35` defaulted the Sparkle signing-key path to a path under the maintainer's home directory, naming a private key store in a file about to become public -- against this repo's own "no public file references a private path" rule. | The default is removed; both `SPARKLE_PRIVATE_KEY` and `SPARKLE_KEY_FILE` must be supplied by whoever runs the script. **The old path remains in git history**, which no change to the working tree can undo -- see "Open" below. |
| F11 | The MCP bearer token was compared with `==` (`McpServer.swift:188`), which exits at the first differing byte. | Constant-time byte comparison. The channel was theoretical on a loopback listener guarding a 122-bit UUID, but a token compare is where a reviewer looks. |
| F12 | Keychain items used `kSecAttrAccessibleAfterFirstUnlock` (`KeychainBox.swift:52`), so a stored API key travels in encrypted backups and device migration. | `...ThisDeviceOnly`. Background reads after first unlock still work, which is what `ctx.secret` needs from timers and rules. |
| F13 (part) | No dependency update bot, and the two workflows pinned `actions/checkout` to different major tags. | Added `.github/dependabot.yml` for the `swift` and `github-actions` ecosystems; `ci.yml` now matches `release.yml` at `checkout@v5`. Pinning actions by commit SHA is still open. |

The three Swift and shell changes could not be compiled or run in this
environment -- there is no Swift toolchain on Linux. CI builds and tests every
push on a macOS runner, so that run is their verification. The Lua suite
(2383 assertions), the README `--check` and the business-content hook all pass
here.

## Fixed -- second round

Each of these was recorded as open above because this environment could not
PROVE the change good. Retrying the blockers cleared four of them: the LuaLS
release installs on Linux after all, and `git ls-remote` resolves a tag to its
commit without any API access.

| ID | What changed | How it was verified |
|---|---|---|

## Fixed -- third round (the three S2 defects that cause real harm)

| ID | Fix | Verification |
|---|---|---|
| N-1 | `isWeekendNight` now anchors 12h back, so a night is named by its OWN day and every instant from noon through 11:59 the next morning resolves to the same night. The anchor moves only at noon, when no countdown is ever armed, so no extra state is needed to pin it. | New case `sleep_schedule_weekend.lua` asserts the user-visible outcome -- whether the machine actually gets slept -- at 00:29:55 and 01:29:55 on a Saturday night, plus that the shift is not STICKY into Sunday evening. It fails against the old code on exactly the assertion the finding describes. |
| N-3 | The session row is keyed to `dateStr(now - dur)`, the day it STARTED, computed before `rollover()`. The app accumulator still flushes under `st.appDate`, which was already correct for the same reason. | New case `usage_stats_midnight.lua` runs a session 23:00 -> 01:00 across a date line and asserts the row lands in the 29th's file and NOT the 30th's. Fails against the old code. |
| H-1 | `LuaState.any` takes a `depth` parameter bounded at 24, and calls `lua_checkstack(L, 4)` before recursing. A depth bound catches a cycle and honest over-nesting with one check; pointer tracking would allocate a set per table read to catch strictly less. | The bound is MEASURED: `registry.describe()` with all 26 features is 6 deep and a nested chain-of-chains effect spec is 7, so 24 leaves ~3x headroom. New Swift test `testCyclicTableDoesNotOverflowTheHost` covers a self-referencing table, a self-referencing ARRAY (the other recursion arm), a mutually recursive pair (which a naive self-check would miss), and 200-deep honest nesting. That test cannot "fail" on regression -- it takes the test process down, exactly as the bug took the app down. |

Suite: 2439 -> 2453 assertions, green shuffled and at the escalated type level.

## Fixed -- fourth round

Seven more, all Lua, all verifiable here. Each carries a block in
`test/cases/audit_regressions.lua`, and each block was checked against the
pre-fix code and fails there on the assertion the finding describes -- the whole
point of grouping them in one case is that a reader chasing any one of them
finds the id, the scenario and the expectation together.

| ID | Fix |
|---|---|
| W-8 | The undo stack entries carry the focused `wid`, and undo/redo refuse (and log) when focus has moved to another window. An unresolvable id -- 0 or nil, which some apps never expose -- counts as a match, because refusing there would break undo for those windows entirely and the old behavior is the better failure when identity is unknown. |
| N-7 | display_off drops an armed countdown on `wake` and `screenUnlock`, the same two events sleep_schedule already hooks for the same reason. Nothing about a fresh wake should inherit the idle state that preceded it. |
| N-2 | `warn2` is clamped to `warn1 - 60` at read time. Clamped rather than validated on purpose: the pair is edited one field at a time, so a transient inversion while the user is still typing must not break the schedule. |
| N-5 | count_down formats the minute count itself and the template takes `%s`, so a legitimate `2.5` timer no longer hits Lua 5.4's `%d` raise -- which `safeFormat` caught only to fall back to the same failing template, leaving the literal `Time (%d min) is up!` on screen. The zh-Hans catalog moved with it; the parity guard caught the half-done state mid-edit, which is exactly its job. |
| N-8 | Clipboard truncation cuts on a UTF-8 boundary (both the 5000-byte entry cap and the 70-byte chooser preview), falling back to the byte cut when the input is not valid UTF-8 -- a pasteboard can hand us anything. |
| N-9 | The history cap is applied on LOAD as well as on record, so lowering `historySize` and restarting no longer leaves the older entries visible and on disk until the next copy. |
| W-2 | `moveToScreen` clamps the NEAR edges too, which the far-edge clamps could not reach: a window hanging off the left of its source screen used to land back ON the source display, and one hanging above landed under the menu bar. |

Suite: 2453 -> 2469 assertions, green shuffled and at the escalated type level.

## Fixed -- fifth round (the two S2s that needed no design call)

Both were left open above for want of a Mac, not for want of a decision. On one:

| ID | Fix |
|---|---|
| W-1 | `windows.screenIndexContaining` is the membership test -- it returns nil for a point on no display -- and `screenIndexAt` now delegates to it, keeping its `or 1` as what it always was: an AIM default for a pointer read between displays. window_snap's `swapBetween` and `displaysWithCounts` ask the membership question, so an off-screen window is no longer dragged onto display 1, counted as swapped, or added to display 1's picker count. The fallback is untouched for locate_pointer and for the active-display read at `init.lua:211`, which do want it. |
| H-5 | Both AX observers register in `.commonModes`, matching every other run-loop source in the seam (`Native+Triggers`, `CapsHyperTap`); the paired `CFRunLoopRemoveSource` calls name the same mode, or the source would never be removed. Window Deck no longer goes deaf for the duration of a menu or modal panel. |

The regression for W-1 drives the FEATURE, not the helper: the defect was in which
question window_snap asked, so a test on `screenIndexContaining` alone would pass
against the pre-fix code. Both of its assertions were mutated independently --
reverting only `swapBetween` and only `displaysWithCounts` each turns its own
assertion red.

H-5 gets a lint instead (`testEverySeamRunLoopSourceUsesCommonModes`): every
`CFRunLoopAddSource`/`RemoveSource` in both source trees must name
`.commonModes`. A per-site test would prove the four sites we just fixed and say
nothing about the fifth, and this failure is invisible at runtime -- nothing
errors, events simply never arrive. Reverting one site to `.defaultMode` fails
it, naming the file and the arguments.

Suite: 2491 -> 2494 Lua assertions; Swift 184 -> 185 tests.

## Fixed -- sixth round (six S3/S4s that needed no design call)

Chosen over the three remaining S2s because those each need a decision from the
author and these need none. Two of them share a root cause and one has a date on
it: the next US DST transition is 2026-11-01.

| ID | Fix |
|---|---|
| H-10 | `timer_daily_at` is a self-rescheduling one-shot chain, not a repeating `Timer` with a fixed 86400s interval. A repeating timer can only add a constant, and a calendar day is 23 or 25 hours at a transition, so every daily schedule slipped an hour at the boundary and stayed slipped until relaunch. `Native.nextDailyFire` re-asks the calendar per fire; the chain re-arms BEFORE invoking Lua, so a `stop(id)` from inside the handler cancels the link that is now live. |
| N-4 | usage_stats' 7-day series steps `store.dayAnchors`, which anchors each day at LOCAL NOON -- far enough from either boundary that an hour of shift cannot cross midnight. Read at 23:00 on a 25-hour day the old fixed-second step landed back on the date it had just left, so the widget rendered that date twice and dropped the seventh. `report.lua` already anchored at noon for exactly this reason; this is the same idiom, now shared. |
| H-4 | The daily log writes through `write(contentsOf:)` under `try?`. `FileHandle.write(_:)` is the bridged ObjC method: on a full disk or a revoked descriptor it raises an NSException that Swift cannot catch, so the app aborted -- on the one path every `ctx.log` call takes. The rollover branch also keys on the DAY alone now; keyed on `logHandle == nil`, a failed open re-entered it for every later line, costing a `createDirectory` plus a full-directory `pruneOldLogs` scan per log line. |
| P-10 | `signals`' shared watcher contains each subscriber in its own `pcall` and logs what it swallows. One throwing rule used to abort the emit for every rule after it -- and since `bindOne` advances `matched` only after `fire()` returns, those rules then re-fired on every later emit instead of once per edge. |
| N-13, P-13 | `urls.getDomain` peels userinfo and port, then REJECTS whatever is left if it is not host-shaped, rather than deleting the offending bytes: `host.com:8443` yielded `host.com8443`, `user:pw@sub.host.com` yielded `userpwsub.host.com`, and `[::1]:8080` yielded `18080`. Those strings are persisted, not merely shown -- the `context` column of the user's daily usage CSV and favicon cache filenames. The localhost guard is equality plus a `.localhost` suffix instead of `find`, which had been rejecting `notlocalhost.com`. |

Test shapes, and why each is what it is:

- **H-10 gets a lint, not an assertion.** The bug is in the WIRING -- a correct
  next-fire time handed to a timer that then ignores it -- so
  `testNoSeamCodeHardcodesADayLength` forbids the literal across both source
  trees, with a roster (empty today) for anything that is genuinely a duration.
  Reverting `timerDailyAt` to the repeating form fails it, naming the file, the
  line and the source text. `testDailyTimerFollowsTheCalendarAcrossDST`
  additionally pins the computation: iterated across the 2026-11-01 fall-back in
  a forced `America/New_York` calendar, every fire must land at 07:30 local, and
  a landmark asserts that a fixed 86400s step is still expected to drift there --
  so a fixture that stops crossing the transition fails loudly instead of
  passing for the wrong reason.
- **N-4's regression is in Swift although the defect is in Lua.** It needs a
  timezone that HAS a transition, and only the Swift suite can force one
  (`setenv("TZ", ...)` + `tzset()` reaches the same C library Lua's `os.date`
  reads). It asserts the naive window repeats a date FIRST -- if forcing TZ ever
  silently fails, both lists come back clean and the real assertion would pass
  proving nothing. Mutated: the seven days come back as `10-27 .. 11-01, 11-01`,
  with `10-26` dropped.
- **H-4's raising-write half gets a class lint** -- every labelled `FileHandle`
  write form either throws or cannot fail, so the rule is that no `.write(`
  takes an unlabelled argument. Both scans self-test their classifier against a
  probe string first, since neither has a real hit left in the tree and an empty
  offender list would otherwise be indistinguishable from a dead pattern. The
  once-per-day rollover half is NOT covered: it needs an unwritable logs
  directory, and `Native.logsDir` is a static with no injection point.
- **P-10's probe makes every subscriber count and then throw**, so the assertion
  does not depend on `pairs()`' undefined iteration order -- a bare loop stops at
  whichever one it reaches first, whatever that turns out to be. Contained-but-
  silent is mutated separately, to prove the log assertion is load-bearing.

Suite: 2494 -> 2507 Lua assertions; Swift 185 -> 189 tests.

## Seventh round (what the post-fix review changed)

The sixth round's changes were then reviewed by a reader that had not written
them, with the diff, this report and CLAUDE.md as its brief. It confirmed the
lifecycle question that worried me most -- `native.stop(id)` runs the canceller
before `freeResource` (`Native.swift:405-411`), and none of the six other
`freeResource` call sites can reach a daily timer, so the chain cannot outlive
the Lua ref it calls -- and found four things worth acting on.

| What | Change |
|---|---|
| **W-1b**, a NEW S2 | `screenOfFrame` returns nil for a midpoint on no display, and `focusedScreen` spells its `or screens[1]` aim default itself, where the choice is visible. The same split W-1 made in index space, now made in frame space. `captureLayout`'s promised skip is live rather than dead. |
| H-4 was half-hardened | `closeFile()` and `seekToEndOfFile()` raise the same uncatchable `NSFileHandleOperationException` as `write(_:)` and sat in the same function, 15 lines from the call that was fixed. Both now use the throwing form. The lint was widened from "no unlabelled `.write(`" to the whole raising family -- a guard scoped narrower than its own failure certifies a file as clean while the siblings sit beside it. |
| H-4's day-key had no voice | Keying the rollover on the day alone means a failed open leaves today's file dead until midnight, where the old `logHandle == nil` test retried (at the cost of a `createDirectory` + full-directory `pruneOldLogs` per line). The trade is right, the silence was not: an empty daily log otherwise reads as "the automation never fired". One `NSLog` on the failed open, and `stdout` still carries everything. |
| Two guards could go blind | `armDaily`'s nil re-arm ended the chain forever with no log line; it now `seamLog`s. The day-length roster keyed on a bare path, so exempting a file would have hidden every later 86400 in it -- it now carries a per-file COUNT, the shape `testEverySeamSubprocessSiteIsClassified` already uses and for the same stated reason. |

W-1b's regression drives `effects.captureLayout`, not `screenOfFrame`: the defect
was which question the caller's answer got substituted for, and both its screens
are deliberately EXTERNAL, since a built-in `screens[1]` would have been filtered
by the unrelated `not s.builtin` guard and hidden the bug behind the wrong
reason. Restoring the fallback turns it red with `2 placements`.

The review's three remaining notes were then taken as well:

- `tab_switcher:209` compared `getDomain(url) == getDomain(choice.subText)`, and
  two nils are EQUAL -- so after `getDomain` learned to reject ports, IPv6
  literals and other shapes it will not vouch for, two unrelated URLs read as
  "same site" and one got stamped onto the other's row. Guarded on the left side
  being non-nil: an unknown domain does not match another unknown domain. NOT
  given a bespoke test -- reaching it means driving a jump callback through the
  chooser, and the property it rests on (which URL shapes return nil) is already
  pinned in the N-13 block.
- The signals error line named neither the signal nor the rule, and the raised
  error carries only a `rules.lua` line shared by every rule on every signal --
  so the log could not answer the question it exists to answer. Each signal now
  learns its REGISTRY key after the table is built (`pushSignal` runs while that
  table is still being constructed, so the key does not exist yet), and
  `subscribe` takes an optional label that `rules.bindOne` fills with
  `rule:<id>`. Both halves are mutation-proven, the second by spying on
  `subscribe` -- a real rule cannot reach that line, because `effects.dispatch`
  is already contained.
- H-5's comments framed `.commonModes` purely as a delivery fix. It is also a
  widening of Lua re-entrancy into menu-tracking and modal loops, which is a
  trade worth making -- every seam timer already fires in `.common`, and a
  handler that never sees the event is the worse failure -- but the file now says
  so, and says to gate a future handler on the mode rather than send the source
  back to `.defaultMode`.

Suite: 2507 -> 2514 Lua assertions; Swift 189 tests.

## Open findings

| ID | Sev | Area | Evidence | Claim | Failure scenario | Fix |
|---|---|---|---|---|---|---|
| F1 | S3 | Capability model | `Native+Network.swift:101-115`; `manifest.lua:108` | `download_file` removes and writes at ANY absolute path, but sits in the `network` tier, not `files`. | A feature or extension declaring only `network` calls `ctx.downloadFile(url, "<any absolute path>")`: the existing file is removed and replaced. The `feature.json` a reader opens to answer "what can this reach" says nothing about file writes. | Either confine the destination to `dataDir`/`cacheDir` unless `files` is also declared, or list `downloadFile` under `files` too. The second is a Lua-only change but shifts a user-visible label onto Bing Daily, so it is a design call left to the author. |
| F4 | S3 | Rules engine | `rules.lua:217-250`; `signals.lua:183-186`; `effects.lua:677` | `fire()` tracks a failure streak and nothing else: no cooldown, no re-entrancy guard, no "this fire was caused by my own effect" suppression. | Two state rules -- "appearance becomes dark -> set light" and "appearance becomes light -> set dark" -- each flip the signal that fires the other, forever, running an AppleScript per flip. The same shape exists with frontmost-app signals and launch/minimize effects. Plausible from the code; not reproduced (needs a Mac). | A per-rule minimum interval, or suppress a fire whose triggering change lands within N ms of that rule's own effect. Alert on oscillation the way the failure streak already alerts. |
| F5b | S3 | Public-repo hygiene | git history | The removed key path (F5) survives in history, where no check of the working tree can see it. | Anyone cloning the public repo can read it in an old revision. | Decide before publishing whether a history rewrite is warranted. The path names a directory, not a secret, so this may be acceptable -- but it should be a decision, not an oversight. |
| F7 | S3 | Supply chain | `Sources/CLua/VENDOR.md`, `include/lua.h` (5.4.7) | Upstream Lua is at 5.4.8 (May 2025); the GitHub mirror also tags 5.4.9. | The embedded engine drifts from upstream fixes. `scripts/test-lua.sh` keeps testing the vendored copy honestly, so this is drift, not breakage. | Re-vendor per VENDOR.md, then `scripts/test-lua.sh` and `swift test`. Dependabot cannot see a vendored tarball; this stays manual. |
| F8 | S3 | Test posture | coverage run, this audit | `adapter.lua` is 31.6% line-covered by the headless suite, and 175 of its 177 function definitions are never called there -- the fake adapter replaces it. Only `swift test` exercises the real seam, and that run measures no coverage. | A regression in an adapter function that only the real bridge calls is invisible to the fast loop, and nobody knows which adapter functions the integration tests actually reach. | Run `swift test --enable-code-coverage` once, and record the Lua-side hits with the same line-hook harness used here. Then add cases for what is dark. |
| F14 | S4 | CI | `scripts/hooks/pre-commit` | The hook and its CI backstop scan for business/strategy words only. Nothing scans for secrets. History is clean today: patterns for OpenAI, AWS, GitHub, Slack, Google and PEM keys all miss. | The first pasted credential is public forever. | Turn on GitHub push protection at publish time, or add a gitleaks step beside the hook backstop. |
| F15 | S4 | Bounded blocking | `CapsHyperTap.swift:262-265` | `hidutil` runs under `waitUntilExit()` on the main thread with no timeout -- the one blocking spawn not on `runProcessCore`. It is rostered by `testEverySeamSubprocessSiteIsClassified`, so it is a known exception, not an oversight. | A wedged `hidutil` freezes the app at Hyper enable. | Route it through `runProcessCore` with a short timeout. |
| F16 | S4 | Maintainability | `manifest.lua:190` (407-line `validate`); `RulesView.swift` (2020 lines); `SettingsView.swift` (1749 lines); `window_fan/lua/init.lua:553` (171-line `refan`) | Four functions and files carry most of the reading cost of the codebase. | Slower review, not defects. | Split `manifest.validate` by section (identity, actions, options, triggers); extract RulesView's editor sheet and sentence builder. |
| F17 | S4 | Test posture | `Tests/` | 40 of 87 platform Swift files are referenced by no test. Most are SwiftUI views, which is expected. The logic-bearing ones are `StatusBar`, `Updater`, `KeychainBox`, `InstallLocation`, `RowListPersistence`, `LenientDecode`, and the `McpHttp` parser, which is reached only through a live socket. | A parser or persistence regression surfaces in the app rather than in CI. | Unit tests for `McpHttp.parse` (caps, malformed headers, chunked refusal) and for `LenientDecode` / `RowListPersistence` on corrupt input. |

## Second-pass findings: functional correctness and host reliability

The first pass asked "can this hurt me". These three readers asked the question it
skipped: "does this compute the right answer". Confidence is the READER's, except
where marked **[re-verified]**, which means I traced the cited lines myself before
recording it -- every S2 here carries that mark.

### Features: time, state and matching (N)

| ID | Sev | Feature | Evidence | Defect and how it bites |
|---|---|---|---|---|
| N-1 ~~open~~ **FIXED** | S2 | sleep_schedule | `init.lua:32,79-84,102` | **[re-verified]** `weekendShift()` reads `isWeekendNight(ctx.now())`, so the shift is recomputed from the CURRENT weekday on every 10s tick and evaporates at midnight. Defaults (`sleepAt=00:30`, shift 60): Sat 23:50 promises sleep at 01:30; at Sun 00:10 the target silently reverts to 00:30 and the machine sleeps an hour before the dialog said it would. The day-rollover guard never trips because the jump is 5400 -> 1800, not a small delta. With `sleepAt=23:45` the mirror case skips that night's enforcement entirely. Fix: resolve the shift from the day the sleep TARGET belongs to, pinned once per cycle. |
| N-2 ~~open~~ **FIXED** | S3 | sleep_schedule | `init.lua:44-45,197` | `warn1Min` and `warn2Min` are range-checked independently, and phase 1 needs `secsLeft <= w1 and secsLeft > w2`. Setting `warn1Min=5, warn2Min=10` (both legal) makes the dismissable dialog unreachable, so the one-time snooze can never be offered -- silently. Clamp `w2 = min(w2, w1 - 1)`. |
| N-3 ~~open~~ **FIXED** | S2 | usage_stats | `init.lua:347-355` | **[re-verified]** `recordSession` calls `rollover()` BEFORE reading `local d = st.appDate`, so a session spanning midnight is written to the WAKE-UP day's CSV with `wake_time > sleep_time`. Awake 23:00 Tue, lock 01:00 Wed writes `23:00:00,01:00:00,120` into Wednesday: Wednesday gains 120 phantom minutes and Tuesday loses the session. (The second-pass write-up also claimed the report plots the span backwards -- it does NOT: `UsageReportView.swift:501-507` already clamps a span whose end precedes its start, and distinguishes a real midnight crossing from clock skew. Corrected on verification; the day-attribution defect is real and is what was fixed.) Use the day the session STARTED, or split the row at midnight. |
| N-4 ~~open~~ **FIXED** | S3 | usage_stats | `init.lua:232-243` | The widget's 7-day series steps by a fixed `86400` in local time, so a DST transition skips or duplicates a day -- `weekTotal` and the average are then computed over the wrong seven days. `report.lua:32` already anchors each day at local noon for exactly this reason; `snapshot()` does not. |
| N-5 ~~open~~ **FIXED** | S3 | count_down | `init.lua:48,88` | `tonumber(text)` accepts `2.5`, but the completion title is `"Time (%d min) is up!"` and Lua 5.4's `%d` raises on a non-integer. `i18n.safeFormat` catches it and falls back to the same English template, which fails identically -- so the notification reads literally `Time (%d min) is up!`. Round the parsed minutes, or use `%s`. |
| N-6 | S2 | site_switcher | `init.lua:62,227`; `Native+Browser.swift:83` | **[re-verified]** The tab-match pattern is a bare domain, and the seam matches it as a plain SUBSTRING of the whole URL (`if tabURL as text contains pattern`; the fake mirrors it with `url:find(pattern, 1, true)`). With sites for both `google.com` and `mail.google.com`, firing google.com focuses an open Gmail tab. `https://notgithub.com.evil/` matches the `github.com` site too. Match on the parsed HOST with a suffix rule. |
| N-7 ~~open~~ **FIXED** | S3 | display_off | `init.lua:139,146` | `remaining = warnSeconds - (ctx.now() - s.warnedAt)` is wall-clock based and there is no `wake`/`screenUnlock` reset, so a sleep/wake gap straddling an armed countdown makes `remaining` hugely negative and blanks the display immediately on wake. `sleep_schedule:222-227` added exactly this handler for the same reason. |
| N-8 ~~open~~ **FIXED** | S3 | clipboard_history | `init.lua:60,81` | Both truncations are BYTE operations (`#text > 5000`, `sub(1, 5000)`, `sub(1, 70)`) though the option label says "chars": 3000 Chinese characters get cut mid-codepoint, so the persisted JSON carries invalid UTF-8 and the chooser row renders a broken glyph. Truncate on a `utf8.offset` boundary. |
| N-9 ~~open~~ **FIXED** | S3 | clipboard_history | `init.lua:47-55` | **[re-verified]** `load()` appends every persisted entry with no cap; the cap lives only in `record()`. Lower `historySize` from 500 to 10 and restart: all 500 entries stay visible, and on disk, until the next copy. Trim at the end of `load()`. |
| N-10 | S4 | 4 features | `break_reminder:37-44`, `display_off:52-57`, `sleep_schedule:56-68`, `bing_daily:155-160` | Every `schedule()` descriptor's label/note is a hardcoded English literal built with `..`, passed through verbatim to `AutomationTimelineView`. On a zh-Hans Mac the Automation Timeline is in English, and `i18n_parity.lua` cannot see it because the strings never reach `ctx.t`. |
| N-11 | S4 | bing_daily | `init.lua:172-175` | The `screenChanged` re-apply calls `ctx.setWallpaper` directly, bypassing `applyWallpaper`, whose own comment says a failed apply "must never read back as a success" -- so a failure after a monitor change is silent. |
| N-12 | S3 | usage_stats | `init.lua:262-268` | The retention sweep walks a FIXED 24-month window, so anything older than `keepMonths + 24` is never deleted, and the once-a-day gate means those months never get a second look. Enumerate the month dirs actually present. |
| N-13 ~~open~~ **FIXED** | S4 | platform.urls | `urls.lua:26` | `getDomain` STRIPS disallowed bytes instead of rejecting: `https://example.com:8443/app` becomes `example.com8443`, which is then recorded verbatim as the `context` column of the user's daily CSV. |
| N-14 | S4 | break_reminder | `init.lua:254-255` | `ctx.setState("workSeconds", ...)` on every 5s active tick is ~17k `NSUserDefaults` writes a day for a counter read twice. Persist on boundaries. |

### Window features (W)

| ID | Sev | Feature | Evidence | Defect and how it bites |
|---|---|---|---|---|
| W-1 | S2 | window_snap | `init.lua:123,145`; `windows.lua:640` | **[FIXED -- fifth round]** `W.screenIndexAt` returned 1 for a point on NO display, so `swapBetween` treats every off-screen window as living on display 1, moves it there, and counts it as swapped; `displaysWithCounts` inflates display 1's picker count the same way. Test membership with a real miss instead of the fallback. |
| W-1b ~~open~~ **FIXED** | S2 | platform.windows, effects | `windows.lua:605`; `effects.lua:940`, `:928` | **[found in review, not by a reader]** The frame-space twin of W-1: `screenOfFrame` returned `screens[1]` for a midpoint on NO display, so `effects.captureLayout` tagged an off-screen window with display 1 and stored `pos` ratios far outside [0,1] -- which nothing downstream clamps. Its own doc four lines up already claimed such a window was skipped, and that skip was dead: `s` was nil only when there were ZERO screens. Reachable the ordinary way -- unplug a display, an app restores a stale frame, save a layout. |
| W-2 ~~open~~ **FIXED** | S3 | platform.windows | `windows.lua:77-84` | `moveToScreen` clamps only the RIGHT and BOTTOM edges despite a doc comment promising a full clamp. Probed: `f={x=-100,...}` onto a right-hand screen lands at x=1340 -- 100px back on the SOURCE display; a negative y lands under the menu bar. The existing keepSize test happens to be saved by the far-edge clamp. |
| W-3 | S3 | window_fan | `init.lua:639` | The capacity gate tests the PRE-assignment high-water index, so a newcomer that would fill a FREED hole is refused as "at capacity" -- permanently, with no slot, colour or border. Gate on the index the newcomer would actually take. |
| W-4 | S3 | window_fan | `init.lua:458` from `:700` | `refan` runs the full back-to-front raise over EVERY member although only the one side whose count changed re-tiles -- so windows that do not move are raised anyway, and an activate-on-raise app (VSCode, Chrome) flashes over the window in view. This is the CLAUDE.md one-raise-per-visible-moment rule. |
| W-5 | S3 | window_deck | `init.lua:1112`, `:606-613` | A focus change inside the 300ms settle window is swallowed and never retried -- the settle timer only clears the flag. Cmd-tab within 300ms of entering the deck leaves the window focused and fronted but NOT the hero, with no hero at all until the next focus event. The suite always flushes the timer first, so it never sees this. |
| W-6 | S3 | window_snap, window_grid | `window_snap:40-44`; `window_grid:77-82` | Half-snaps, user placements and `placeSpan` have no `f.fullscreen` guard, though window_snap's own header claims fullscreen is exited first. On a fullscreen window the shortcut silently does nothing. Either add the prologue `window_modal:58` already has, or delete the claim. |
| W-7 | S3 | window_snap | `init.lua:52` | `toggleMax` decides "already maximized" by exact float equality against the visible frame, so an app that quantizes its accepted frame (Terminal to character cells) never reaches the 75% state. Compare with a tolerance. |
| W-8 ~~open~~ **FIXED** | S2 | window_modal | `init.lua:71-89` | **[re-verified]** The undo stack stores `{x,y,w,h}` with NO window identity, and `undo()` applies the popped frame to whatever is focused NOW. Snap window A, click window B, press `[`: B is flung to A's old geometry and A stays snapped. Store the wid alongside each entry. |
| W-9 | S4 | window_modal | `init.lua:128` | A raw English direction token is interpolated into a translated template: zh-Hans renders "left 方向没有屏幕". |
| W-10 | S4 | window_modal | whole file | The only window feature with ZERO `ctx.log` calls, though it is a stateful modal layer -- enter/exit, undo, redo and the no-screen refusal are all silent, against the CLAUDE.md audit-trail rule. |
| W-11 | S4 | window_deck | `init.lua:1173-1174` | The `ignore` outcome is the only one of `focus.classify`'s five with no log line, so a member whose app misses the AX ceiling produces silence between states. |
| W-12 | S4 | pointer_follows_window | `feature.json` | The description promises the pointer follows Deck moves, but Deck writes via `setFrameFor`, which `window_ops.lua:86-94` documents as deliberately pointer-follow-free. Fan is likewise not covered. The feature also logs on enable but has no `stop`. |
| W-13 | S4 | window_fan | `init.lua:730,735,741,757` | `resyncFocus` makes THREE full `ctx.window.list()` AX enumerations per call and is called twice per focus event -- nine AX walks per cross-app switch, on a path the module's own comment calls "dear". |
| W-14 | S4 | window_deck | `window_history.lua:16-23`; `window_deck:64,141,1247` | Comments assert "every list_windows() re-mints the numeric ids", which `Native+Windows.swift:317-321` no longer does. The stale belief drives four to five AX enumerations per hero swap where one would do. |
| W-15 | S4 | window_rewind | `window_history.lua:175` | `undoLast` silently skips a window whose before-frame centre is off every current screen: `n` is not incremented and nothing is logged, so the flash reports a smaller count with no explanation. |

### Swift host reliability (H)

| ID | Sev | Evidence | Defect and how it bites |
|---|---|---|---|
| H-1 ~~open~~ **FIXED** | S2 | `LuaState.swift:378,396-397,414-415` | **[re-verified]** `LuaState.any` recurses into nested tables with NO depth limit and NO cycle detection. A feature or extension that hands any table-reading binding a self-referencing table (`local t={}; t.me=t` into `hud_show`, `http_get` headers, `run_process` args) recurses until the Swift stack overflows: the host dies with no log line and no Lua traceback. Thread a depth bound and/or track visited pointers. |
| H-2 | S3 | `LuaState.swift` (zero `lua_checkstack` calls in the file) | **[re-verified: 0 hits]** Every reader and pusher relies on the 20 slots `LUA_MINSTACK` guarantees. `any` consumes ~2 slots per nesting level, so a ~10-deep table pushes past the reserved area, and in a release build `api_check` is compiled out -- silent stack corruption rather than an error. |
| H-3 | S3 | `Native.swift:407-410` (~40 call sites) | `luaError` calls `lua_error`, which longjmps out of the enclosing Swift frame, so ARC releases and `defer` blocks never run -- e.g. `deckWidgetShow`'s per-field `defer { lua_settop(L, -2) }` leaks a Lua stack slot and the Swift locals on every raise. `Native+Panels.swift:649` also calls `lua_getfield` with no type check on arg 1. |
| H-4 ~~open~~ **FIXED** | S3 | `Native.swift:330-336` | The daily-log handle uses the exception-RAISING ObjC `write(_:)`, uncatchable from Swift: a full disk or an unwritable Application Support aborts the app on a path every `ctx.log` takes. And if the open returns nil while `logDay` is set, every later line re-runs `createDirectory` + `pruneOldLogs` (a full directory scan). |
| H-5 | S2 | `Native+Windows.swift:915,923,987,1032` | **[FIXED -- fifth round]** Both AX observers added their run-loop source in `.defaultMode`, while every timer in the seam uses `.common` (and `Native+Triggers.swift:211` uses `.commonModes`). While the menubar menu is tracking or a modal panel is up, focused-window-changed and window-moved notifications are NOT DELIVERED -- Window Deck misses hero promotions for the duration, then resumes with stale state. |
| H-6 | S3 | `Native+Windows.swift:183-336` | `listWindows` mutates three pieces of shared state across a long run of synchronous AX reads with no re-entrancy guard; an interleaved inner listing makes windows unresolvable, which is the exact bug the stable-id rework was written to fix. |
| H-7 | S3 | `Native+Panels.swift:298,658` | `LuaState.bool` returns a non-optional, so `LuaState.bool(L, 7) ?? true` is dead code and the documented `true` default can never apply -- an omitted `heroOn` silently becomes false. Latent today because the adapter always passes it. |
| H-8 | S3 | `Native+Panels.swift:661` | `ref(_:)` calls `makeRef` on whatever `lua_getfield` produced, including nil, so an omitted callback yields `LUA_REFNIL` and every drag writes "attempt to call a nil value" into the daily log -- unthrottled, and invisible to the DEBUG ref-leak check. |
| H-9 | S3 | `AppRelaunch.swift:138-140` | `try? task.run()` discards a spawn failure and the process exits anyway: if the relaunch helper cannot spawn, the app simply vanishes after the install-location move, with no alert and nothing in the log. |
| H-10 ~~open~~ **FIXED** | S3 | `Native+Triggers.swift:161` | `timer_daily_at` uses a fixed 86400s repeat, not a calendar recurrence, so every daily schedule drifts an hour across a DST transition and stays drifted until restart. |
| H-11 | S4 | `LuaState.swift:122,130` | `run`/`runFile` use `LUA_MULTRET` and never pop, so boot permanently parks one value on the stack. Harmless once; unbounded if a value-returning chunk ever runs on a poll path. |
| H-12 | S4 | `LuaState.swift:194` | `lua_getfield(L, -1, function)` runs on whatever `require()` returned with no table check; a module returning a non-table would longjmp past the `lua_settop` restore and leave the stack unbalanced for every later host call. |
| H-13 | S4 | `CapsHyperTap.swift:264-265` | The roster entry covers the unbounded main-thread wait, but the discarded `try? p.run()` is separate: `waitUntilExit()` is then called on a process that never launched. |

### Lua platform core (P)

The third and last gap. Two passes had read this layer for SECURITY -- seam
integrity, the capability gate, error containment, teardown -- and none had ever
checked its LOGIC. It is the largest subsystem in the tree (rules + signals +
effects is ~1,800 lines, `manifest.validate` alone is 407), and F4 had surfaced
from incidental reading rather than a systematic one, which is usually how a
lone finding in an unaudited subsystem appears. Every S2 below was re-probed by
hand before being recorded.

| ID | Sev | Module | Evidence | Defect and how it bites |
|---|---|---|---|---|
| P-1 ~~open~~ **FIXED** | S2 | effects | `effects.lua:741` | **[re-verified by probe]** `chain` is listed in `CATALOG_ORDER` but declared no `label`, so `catalog()` handed i18n a nil default and the Rules > Do dropdown rendered the literal key `rules.effectKind.chain`. ENGLISH builds only -- zh-Hans has a translation -- which is exactly the class `i18n_parity` structurally cannot catch: it checks translations against English sources, and here the English source was the missing half. Fixed, plus an assert in `catalog()` so a future kind cannot repeat it. |
| P-2 | S2 | registry, manifest | `registry.lua:684-709`; `manifest.lua:302-306`; `triggers.lua:38-44` | **[re-verified by probe]** Two joined defects. `manifest.validate` accepts a `defaultTrigger` the SEAM rejects -- it checks only `type(spec) == "table" and spec.type`, never routing through `triggers.validate` -- although the comment at `triggers.lua:38` promises rejection happens "at validate time, before a bad spec ever persists or binds". Probe: `mods = {"cmmd"}` passes the manifest and is refused at bind. And when a service's `start(ctx)` has already succeeded and the later `bindAction` throws, the failure path tears down the scope but never calls `m.stop()` -- and the subsequent disable cannot either, since `unbindFeature` returns early on `not b`. Any non-ctx state `start()` took is stranded for the process lifetime. |
| P-3 | S2 | rules | `rules.lua:128-160,512-567` | **[re-verified by probe]** A duplicate rule id in stored JSON parks the second copy, and `describe()` then emits TWO rows carrying the same `id`. `RulesView.swift:88-98` keys `List(selection:)`/`ForEach` on that id, so selection, edit, Test and delete resolve to the wrong row. This is the same hazard `registry.lua:194-199` already guards against for load-failure rows. |
| P-4 | S3 | registry | `registry.lua:922-963` | `swapTriggers` bypasses the automatable gate `setTrigger` enforces, so an automated spec written onto a non-automatable action is silently refused at load and that action falls back to its DEFAULT -- the combo the other side just took. Probe: after a swap, two actions bind `cmd+j`, one press fires both, and the schedule is gone. The comment claims the swap is "safe by construction". Reachable through the Lua/host API, not today's UI. |
| P-5 | S3 | triggers | `triggers.lua:78-83` | `combo()` canonicalizes neither modifier aliases nor case, so `{"cmd"}` and `{"command"}` compare unequal and both bind -- while `KeyModifier.swift` maps both to the same physical key. The CI collision guard rides the same function, so a new feature using a long alias ships green. `hotkeys.lua` and `registry_view.lua` both canonicalize; this is the outlier. |
| P-6 | S3 | manifest | `manifest.lua:473-554` | The option loop never checks default-vs-type, default-vs-`values`, default-vs-min/max, `min <= max`, or duplicate keys. Probe: all eight incoherent shapes are accepted. `SettingsView.swift:1413` builds `Int(min)...Int(max)`, so a `min > max` option traps at runtime. All 26 shipped features are clean, so this is a missing guard on the extension path rather than a live defect. |
| P-7 | S3 | rules, registry | `rules.lua:63-67`; `registry.lua:742-765` | The parking comment promises a parked rule re-activates "once its target returns (the next load re-validates it)", but `registry.reload()` never calls `rules.loadFromSettings()` -- its only caller is boot. So a rule parked at boot stays greyed after Reload Features or the MCP `reload` tool; only a relaunch revives it. |
| P-8 | S3 | rules | `rules.lua:76-96` | `parkReason` blames any remaining park with a command effect on the target feature, including parks with nothing to do with it -- probe: a duplicate-id park reports "feature 'X' isn't available" when X is loaded and enabled. The comment frames the heuristic as guarded against exactly this misattribution. |
| P-9 | S3 | rules | `rules.lua:617` | `rules.sentence` uses `enter and on.becomes or on.leaves`, the idiom `bindOne`'s own comment identifies as wrong because it collapses to `leaves` when `becomes` is boolean `false`. Unreachable today (state targets must be non-empty strings), but it is the second half of an invariant the code claims to hold. |
| P-10 ~~open~~ **FIXED** | S3 | signals | `signals.lua:76-79` | The subscriber fan-out has no per-subscriber pcall, and `rules.bindOne` advances `matched` only AFTER `fire()` returns. One throwing rule aborts the emit for every other rule on that signal AND re-fires on every subsequent emit instead of once per edge. `effects.dispatch` is contained; `adapter.alert` and `effects.describe` on the fire path are not. |
| P-11 ~~open~~ **FIXED** | S4 | effects | `effects.lua:844` | **[re-verified by probe]** `dispatch` indexed `node.kind` one line ABOVE its own `node and node.kind` nil guard, so a nil node threw against the documented "never throws" contract. Guard moved before the lookup. |
| P-12 | S4 | favicons | `favicons.lua:188-228` | A site answering `/favicon.ico` with non-image bytes leaves a cached file `fileExists()` sees and `looksLikeImage()` rejects, and nothing removes it -- so that domain's icon can never appear again. Permanent self-inflicted cache poisoning, one file per bad host. |
| P-13 ~~open~~ **FIXED** | S4 | urls | `urls.lua:46` | `getDomain` rejects any host CONTAINING "localhost" (find, not equality), so `notlocalhost.com` yields nil; and it folds a port into the name, so `host.com:8080` becomes `host.com8080` -- which then becomes a favicon cache filename. |

**What this pass confirms is correct**, probed rather than assumed: i18n's
never-throws contract across six argument/slot mismatch shapes; `json`'s `%z`
NUL handling on the vendored 5.4.7 (so `"zebra"` is not mangled) and its loud
rejection of mixed array/object tables; state-signal edge semantics (seeded at
bind, no re-fire on an equal value, `becomes` does not fire on leave); the chain
effect's deliberate non-atomicity with the failed steps named in the note;
`triggerFor`'s automatable refusal on the LOAD path; `registry_view`'s modifier
canonicalization; and `capscan`'s literal stripping across block comments, long
brackets, escapes and the `os["execute"]` bracket form.


### What the second pass confirms is correct

Worth recording alongside the defects, since these were checked and hold: the
one-shot cancel race is correctly ordered (`Native+Callbacks.swift:70-81`); every
armOneShot runs before its completion can fire; no path reaching `LuaState` off
the main thread was found; `call`'s stack balance is restored on all three paths
(`LuaState.swift:189-206`); a failed hotkey registration leaves no half-registered
state (`HotkeyCenter.swift:73`); the CGEvent tap re-arms on both disable reasons
(`CapsHyperTap.swift:282-285`); `runProcessCore`'s exactly-once completion holds
under the throw path; `tileSlots`' partial-last-row math was probed at n=5/7/8 and
distributes gaps evenly with no dead cells; the coordinate system is one
convention end to end (`axRect`, `Native+Windows.swift:403-406`); `window_history`
re-entrancy is properly gated so there is no double-undo loop; and no window
action sets `automatable = true`, so no automated trigger can fire a
live-focus-reading action.

## Verified true

These hold, each with the line that proves it. For a repo about to go public
this list is half the deliverable: it is the part a reader can check.

**MCP endpoint.** Loopback is enforced at the listener through
`requiredLocalEndpoint`, not by a check that could be bypassed
(`McpServer.swift:60-63`). Gate order is Origin, then path, then method, then
token (`:177-188`). POST only; 405 with `Allow` otherwise. Header cap 16 KiB,
body cap 1 MiB, chunked bodies refused (`McpHttp.swift:37-38`). The token is a
v4 UUID minted once and kept stable (`Boot.swift:128-135`). The tool set is
reflective only -- `describe_feature`, `get_extension_guide`,
`get_extensions_dir`, `list_api`, `list_features`, `read_log`, `reload`,
`run_action`, `set_enabled`, `validate_extension`. There is no eval tool.

**`exec` tier.** Absolute executable path required, argv array with no shell in
the loop (`Native+Process.swift:378-395`); NUL bytes rejected in Lua before the
call (`adapter.lua:926`); stdin pinned to `/dev/null` and cwd to `/`
(`:192-193`); 1 MiB per-stream capture ceiling; a 60 s SIGTERM watchdog with a
hard stop behind it; every launch logged with its full argv. A first-party
feature cannot declare it (`feature_capabilities.lua`).

**Script builders.** AppleScript literals go through `escAppleScript`
(`Native+Browser.swift:58`); JXA strings through a JSON-serialized literal
(`:474`); browser names are checked against a fixed set (`:353`); tab and window
ids are typed integers; `adjust_volume` takes an `Int` (`Native+Audio.swift:19`);
`set_appearance` validates its mode (`Native+System.swift:254`). Chromium
launches pass `--app=` and `--profile-directory=` as bound argv entries, never a
shell string (`Native+Browser.swift:322-325`). Every synchronous AppleScript
carries a `with timeout` ceiling (`Native+AppleScript.swift:91`).

**Debug surface.** The Lua eval channel and the `.env` secret seeder are both
inside `#if DEBUG` (`DebugControl.swift:38`, `DevEnv.swift:1`). The seven
`HAMMERDECK_*` environment knobs (`CONTROL_DIR`, `DUMP_CATALOG`, `DUMP_KEYS`,
`LUA_DIR`, `NO_FIRSTRUN`, `NO_MOVE_PROMPT`, `RESOURCE_DIR`) change no security
behavior in a release build.

**Secrets and privacy.** The OpenAI key lives in the login Keychain under
service `com.hammerdeck.secrets` and reaches the request only as a header
(`ctx.lua:261`, `text_actions/lua/init.lua:114-128`); the request body is the
system prompt, the selected text and the model name, and nothing else. Password
Generator draws from `SecRandomCopyBytes` via `ctx.randomInt`
(`password_generator/lua/init.lua:10,37`, `Native+Input.swift:197-205`) and logs
only the length and the class count (`:75`). Clipboard History honors four
concealed and transient pasteboard types (`Native+Storage.swift:65-69`),
truncates entries at 5000 characters and caps the history at the user's option
(`init.lua:20,60-66`). Usage Stats quotes CSV fields, so a comma in a window
title cannot shift columns (`store.lua:26-28`). Insert Date/Time pcall-guards
`os.date` against a bad custom pattern (`init.lua:63`). Logs prune at 14 days
(`Native.swift:341`).

**Outbound network.** Exactly the four kinds SECURITY.md lists, and no fifth:
Sparkle; Bing Daily (`bing_daily/lua/init.lua:95,126`); Text Actions AI
(`text_actions/lua/init.lua:126`); favicons (`platform/lua/favicons.lua:74`).
The only Swift transport is `Native+Network.swift` (`URLSession` at `:32` and
`:108`). No telemetry, analytics or crash-reporting path exists.

**Error containment.** Trigger fires are pcall-wrapped with a failure-streak
alert (`registry.lua:468-480`); start, stop, option-change and extension load are
wrapped (`:165,190,718,1044,1060`); rules load, bind and effect dispatch are
wrapped (`rules.lua:147,295`, `effects.lua:850`). On the Swift side every
callback runs under `lua_pcallk` with the stack reset and the error routed to a
sink rather than propagated (`LuaState.swift:262-272`), every bridge entry
asserts the main thread, and every async one-shot hops to main before touching
Lua (`Native+Callbacks.swift:70-80`).

**Capability gate.** A withheld method becomes a raising stub naming the feature,
the method, the capability and the file to edit (`ctx.lua:697-727`). The tier
table has one home (`manifest.lua:108-137`). `capscan.RAW_REACH` covers the file
stdlib as `files` and `os.execute` / `io.popen` / `package.loadlib` as withdrawn
(`capscan.lua:47-59`).

**Loader and extensions.** The custom searcher sits at position 2 and claims only
the `platform.`, `features.` and `extensions.` prefixes, so a user extension
cannot shadow a platform module or a stdlib name (`app/loader.lua`). An extension
id colliding with a built-in is a load failure, not a silent shadow: the
`assert` at `registry.lua:148` refuses it, the pcall at `:190` quarantines the
extension rather than the app, and the failure record deliberately does not
reuse the colliding id (`extensions.lua:100` proves it). `json.decode` returns `nil, err` rather than raising, caps
nesting at 64, and rejects trailing garbage (`json.lua:148-151,178`).

**Crash sites.** All 11 `fatalError` calls are `init(coder:)` stubs on
programmatic panels. All 6 forced casts are the canonical CoreFoundation casts,
each gated by a `CFGetTypeID` check above it (`Native+Windows.swift:419-449`,
`:1026`). Every `@unchecked Sendable` box sits behind a lock, with a comment
saying which.

**Release pipeline.** The signing certificate is decoded into `RUNNER_TEMP`,
imported, and deleted (`release.yml:45-71`); the keychain password is a fresh
UUID; the EdDSA public key is baked into the packaged Info.plist
(`package.sh:67,188`); the release workflow runs only on `v*` tags, so fork and
pull-request runs never see the secrets. `Package.resolved` is tracked
deliberately, so re-running a tag notarizes the same updater.

**Suite health.** 2439 assertions pass on the vendored 5.4.7 engine, including
under `--shuffle`, and every case is checked for leaked native handles. A runtime
probe over the whole run recorded zero accidental global writes and zero reads of
undefined globals from `app/` code. The README catalog is in sync. Git history
carries no credential files and no key-shaped strings.

## Test posture

| Measure | Value |
|---|---|
| Platform Swift lines / feature Swift | 26,304 / 2,166 |
| Lua platform / Lua features | 9,006 / 8,320 |
| Swift tests | 183 across 14 files (16 call `requireUITests()` and skip without `HAMMERDECK_UI_TESTS`) |
| Lua suite | 2,439 assertions across 87 case files |
| Approximate Lua line coverage (headless) | 86.1% |
| Platform Swift files referenced by no test | 40 of 87 |

Lowest-covered Lua, headless suite only:

| File | Coverage | Uncalled function definitions |
|---|---|---|
| `app/hammerdeck.lua` (boot) | 0% | not reachable headless |
| `platform/lua/adapter.lua` | 31.6% | 175 of 177 |
| `features/window_switcher` | 72.7% | 2 |
| `features/window_rewind` | 73.5% | 0 |
| `features/plain_paste` | 74.5% | 0 |
| `features/break_reminder` | 74.9% | 0 |
| `features/sleep_schedule` | 81.2% | 0 |
| `features/text_actions` | 82.3% | 1 |
| `features/window_modal` | 84.5% | 17 |

Method: a `debug.sethook` line-and-call hook over `test/run.lua` on the vendored
interpreter. Line percentages are approximate -- candidate lines exclude blanks,
comments and purely structural lines. Uncalled-function counts are exact, and
count every function definition including local helpers and anonymous callbacks,
which is why `adapter.lua` reports 177 where it exports 116.

## Remaining passes (need a Mac)

1. `swift build` and `scripts/test-swift.sh` on this head -- the three Swift and
   shell changes above have not been compiled anywhere but CI. Then
   `swift test --enable-code-coverage`, and export the Lua-side coverage of
   `adapter.lua` from the integration run (F8).
2. `HAMMERDECK_UI_TESTS=1 swift test` with Accessibility granted to the terminal,
   away from the keyboard.
3. The functional matrix: 26 features x enable, each action, each trigger type,
   each option, one error path, disable. While there, confirm F3 by watching
   which TCC prompt actually appears on first Hyper use.
4. Reproduce F4 with the two appearance rules; measure how fast it spins and
   whether the failure-streak alert ever fires.
5. Instruments: main-thread hangs over a working day, AX call cost per Window Fan
   and Window Deck tick, memory growth with Clipboard History and Usage Stats on.
6. InstallLocation: the move out of Downloads with an existing target, from a
   translocated launch, and interrupted mid-move.
